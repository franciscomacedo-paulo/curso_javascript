/*
    *NESTA AULA VAMOS FALAR DO OPERADOR TERNARIO, QUE É UMA FORMA SIMPLICADA DO OPERADOR RELACIONAL IF...ELSE 
*/
let idade = 18
let stado = "a"
let verificador = (idade >= 18 && stado == "A" ) ? "Acesso Permitido" : "Acesso Negato";
console.log(verificador)